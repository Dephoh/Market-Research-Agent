{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE_PATH=$(cat | jq -r '.tool_input.file_path // empty'); if echo \"$FILE_PATH\" | grep -q 'raw_research/.*\\.md$'; then python3 \"$CLAUDE_PROJECT_DIR/.claude/validators/check_research_output.py\" \"$FILE_PATH\" 2>&1 || true; fi",
            "timeout": 30,
            "statusMessage": "Validating research output..."
          }
        ]
      }
    ]
  }
}
